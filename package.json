{
  "name": "focus",
  "version": "2.2.0",
  "description": "Focus is a Bootstrap theme for creative people.",
  "author": "webvy",
  "browserslist": [
    ">= 0.5%",
    "last 2 major versions",
    "not dead",
    "Chrome >= 60",
    "Firefox >= 60",
    "Firefox ESR",
    "iOS >= 12",
    "Safari >= 12",
    "not Explorer <= 11"
  ],
  "scripts": {
    "clean": "del-cli dist",
    "purgecss": "purgecss -c purgecss.config.js -o dist/assets/css/",
    "build": "npm run clean && eleventy && npm run purgecss",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.1.0",
    "autoprefixer": "^10.4.7",
    "del-cli": "^5.0.0",
    "eleventy-sass": "^2.1.0",
    "postcss": "^8.4.14",
    "purgecss": "^4.1.3"
  },
  "dependencies": {
    "@midzer/tobii": "^2.4.0",
    "bootstrap": "^5.2.0"
  }
}
